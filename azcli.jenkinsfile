pipeline { 
agent{label 'ALL'}
stages {
	stage('git cloneing') {
		steps {
		git url: 'https://github.com/vikashpudi/shopizer.git' , branch: 'azcli'
		 
		}
	    }
	
        stage ('') {
            steps {
                sh 'chmod 777 '
                sh './azcliinstall.sh'
        }
		}
        stage ('') {
            steps {
                sh './azcliforjenkins.sh'
        }
		}
         
 }
  }