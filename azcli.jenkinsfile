pipeline { 
agent{label 'ALL'}
stages {
	 stage ('azcli install') {
            steps {
                sh 'chmod 777 azcliinstall.sh'
                sh './azcliinstall.sh'
        }
		}
        stage ('azcli running') {
            steps {
                sh 'chmod 777 azcliforjenkins.sh'
                sh './azcliforjenkins.sh'
        }
		}
         
 }
  }