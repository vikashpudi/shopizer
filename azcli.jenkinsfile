pipeline { 
agent{label 'ALL'}
stages {
	stage('git cloneing') {
		steps {
		git url: 'https://github.com/vikashpudi/shopizer.git' , branch: 'azcli'
		 
		}
	    }
	
        stage ('azcli install') {
            steps {
                sh 'chmod 777 '
                sh './azcliinstall.sh'
        }
		}
        stage ('azcli running') {
            steps {
                sh './azcliforjenkins.sh'
        }
		}
         
 }
  }